-- ============================================================================
-- materials.style — той самий 6-значеннєвий стиль блоку, що вже для callout
-- (Без виділення/Інфо/Порада/Увага/Успіх/Особливий). Дублюємо CHECK, а не
-- виносимо в спільний тип на рівні БД — materials і tasks окремі таблиці,
-- і це просто значення-константа, не FK.
-- ============================================================================

alter table public.materials add column style text
  constraint materials_style_check check (
    style in ('none', 'info', 'tip', 'warning', 'success', 'special')
  );
