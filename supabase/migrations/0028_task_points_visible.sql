-- ----------------------------------------------------------------------------
-- Показ балів студенту заздалегідь (до виконання вправи) — перемикач на
-- рівні КОЖНОЇ окремої задачі, не курсу/тесту загалом. false = бали видно
-- студенту лише ПІСЛЯ перевірки (як і зараз показується score%), не до.
-- Самі бали на кожен елемент вправи (напр. statements[].points у
-- true_false) живуть у вже гнучкому tasks.config — нова колонка не
-- потрібна, лише цей перемикач видимості.
-- ----------------------------------------------------------------------------
alter table public.tasks add column points_visible boolean not null default false;
